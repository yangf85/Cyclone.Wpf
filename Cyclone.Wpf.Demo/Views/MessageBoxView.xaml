<UserControl
    x:Class="Cyclone.Wpf.Demo.Views.MessageBoxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cy="https://www.cyclone/wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Cyclone.Wpf.Demo.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel>
        <GroupBox Padding="10" Header="Notification">
            <cy:SpacingUniformGrid Rows="1">
                <Button Background="{DynamicResource Background.Info}" Click="Button_Click" Content="Infomation" />
                <Button Background="{DynamicResource Background.Success}" Click="Button_Click" Content="Success" />
                <Button Background="{DynamicResource Background.Error}" Click="Button_Click" Content="Error" />
                <Button Background="{DynamicResource Background.Warning}" Click="Button_Click" Content="Warning" />
                <Button Click="Button_Click" Content="Default" />
            </cy:SpacingUniformGrid>
        </GroupBox>
        <GroupBox Header="Alert">
            <Button Click="Alert_Click" />
        </GroupBox>
    </StackPanel>
</UserControl>