<UserControl x:Class="Cyclone.Wpf.Demo.Views.FormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cy="https://www.cyclone/wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Cyclone.Wpf.Demo.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=local:FormViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <cy:SpacingStackPanel>
            <cy:Form>
                <cy:FormItem IsRequired="True" Label="用户名:">
                    <TextBox />
                </cy:FormItem>
                <cy:FormItem IsRequired="True" Label="密码:">
                    <PasswordBox />
                </cy:FormItem>

                <cy:FormItem Label="电子邮箱:">
                    <TextBox />
                </cy:FormItem>

                <cy:FormItem>
                    <Button Width="100" HorizontalAlignment="Left" Content="提交" />
                </cy:FormItem>
            </cy:Form>

            <cy:Form DefaultLabelPosition="Top">
                <!--  表单项将默认使用顶部标签  -->
                <TextBox cy:Form.IsRequired="True" cy:Form.Label="用户名:" />
                <PasswordBox cy:Form.IsRequired="True" cy:Form.Label="密码:" />
            </cy:Form>
        </cy:SpacingStackPanel>
    </Grid>
</UserControl>