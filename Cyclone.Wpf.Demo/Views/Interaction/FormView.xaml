<UserControl
    x:Class="Cyclone.Wpf.Demo.Views.FormView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cy="https://www.cyclone/wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Cyclone.Wpf.Demo.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:FormViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel Background="Gray">
            <cy:FormItem Label="FirstName" SharedName="Form">
                <TextBox Text="{Binding Data.FirstName}" />
            </cy:FormItem>
            <cy:FormItem Label="LastName" SharedName="Form">
                <TextBox Text="{Binding Data.LastName}" />
            </cy:FormItem>
            <cy:FormItem Label="Age" SharedName="Form">
                <cy:NumberBox Value="{Binding Data.Age}" />
            </cy:FormItem>
            <cy:FormItem Label="City">
                <TextBox Text="{Binding Data.City}" />
            </cy:FormItem>
            <cy:FormItem Label="City">
                <CheckBox Content="{Binding Data.City}" />
            </cy:FormItem>
        </StackPanel>
    </Grid>
</UserControl>