<UserControl x:Class="Cyclone.Wpf.Demo.Views.HintBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cy="https://www.cyclone/wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Cyclone.Wpf.Demo.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=local:HintBoxViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <cy:SpacingStackPanel>
            <cy:HintBox SelectionChanged="HintBox_SelectionChanged">
                <cy:HintBoxItem HintText="Demon Hunter" />
                <cy:HintBoxItem HintText="Death Knight" />
                <cy:HintBoxItem HintText="Blade Master" />
            </cy:HintBox>
            <cy:HintBox x:Name="Hint" ItemsSource="{Binding Data}">
                <cy:HintBox.ItemTemplate>
                    <DataTemplate>
                        <cy:HighlightTextBlock HighlightForeground="Red"
                                               QueriesText="{Binding Path=InputText, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=cy:HintBox}}"
                                               SourceText="{Binding HintText}" />
                    </DataTemplate>
                </cy:HintBox.ItemTemplate>
            </cy:HintBox>
        </cy:SpacingStackPanel>
    </Grid>
</UserControl>