<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:Cyclone.Wpf.Converters"
    xmlns:ctl="clr-namespace:Cyclone.Wpf.Controls">
    <ResourceDictionary.MergedDictionaries>
        <!--<ResourceDictionary Source="pack://application:,,,/Cyclone.Wpf;component/Themes/BasicTheme.xaml" />-->
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="MultiComboBoxItem.SelectedCheckBoxStyle" TargetType="CheckBox" />

    <Style x:Key="MultiComboBox.OpenButtonStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Focusable" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="RootGrid" Background="Transparent">
                        <Path
                            x:Name="Indicator"
                            Width="18"
                            Height="18"
                            Margin="{DynamicResource Margin.Right}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Data="M 0 0 L 6 6 L 12 0 Z"
                            Fill="{DynamicResource Foreground.Default}"
                            Stretch="Uniform" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootGrid" Property="Background" Value="{DynamicResource Background.Hover}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Indicator" Property="Data" Value="M 0 6 L 6 0 L 12 6 Z" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="MultiComboBoxItem.ControlTemplate.Basic" TargetType="ctl:MultiComboBoxItem">
        <Border
            x:Name="RootBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <CheckBox
                    x:Name="SelectedCheckBox"
                    Grid.Column="0"
                    Margin="3,0"
                    VerticalAlignment="Center"
                    IsChecked="{Binding Path=IsSelected, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                    Visibility="Collapsed" />
                <ContentPresenter
                    x:Name="ContentContainer"
                    Grid.Column="1"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Content="{TemplateBinding Content}"
                    ContentStringFormat="{TemplateBinding ContentStringFormat}"
                    ContentTemplate="{TemplateBinding ContentTemplate}"
                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Path=IsShowCheckBox, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ctl:MultiComboBox}}" Value="True">
                <Setter TargetName="SelectedCheckBox" Property="Visibility" Value="Visible" />
            </DataTrigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource Background.Selected}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Background.Hover}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="MultiComboBox.DeselectButtonStyle" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="RootGrid" Background="Transparent">
                        <Path
                            x:Name="Indicator"
                            Width="14"
                            Height="14"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Data="M558.933333 529.066667l285.866667 285.866666-29.866667 29.866667-285.866666-285.866667-285.866667 285.866667-29.866667-29.866667 285.866667-285.866666L213.333333 243.2l29.866667-29.866667 285.866667 285.866667L814.933333 213.333333l29.866667 29.866667-285.866667 285.866667z"
                            Fill="{DynamicResource Foreground.Default}"
                            Stretch="Uniform" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Indicator" Property="Fill" Value="{DynamicResource Icon.Foreground.Hover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MultiComboBox.PresentItemStyle" TargetType="ctl:MultiComboBoxItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ctl:MultiComboBoxItem">
                    <Grid Margin="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ContentPresenter VerticalAlignment="Center" Content="{TemplateBinding Content}" />
                        <Button
                            x:Name="DeselectButton"
                            Grid.Column="1"
                            Command="{x:Static ctl:MultiComboBox.DeselectItemCommand}"
                            CommandParameter="{TemplateBinding Content}"
                            Style="{StaticResource MultiComboBox.DeselectButtonStyle}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="0.5" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MultiComboBoxItem.Style.Basic" TargetType="ctl:MultiComboBoxItem">
        <Setter Property="Template" Value="{StaticResource MultiComboBoxItem.ControlTemplate.Basic}" />
        <Setter Property="Height" Value="{StaticResource Height.Horizontal.Item}" />
        <Setter Property="Background" Value="{DynamicResource Background.Global}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <ControlTemplate x:Key="MultiComboBox.ControlTemplate.Basic" TargetType="ctl:MultiComboBox">
        <Border
            Name="RootBorder"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ItemsControl
                    Grid.Column="0"
                    ItemContainerStyle="{StaticResource MultiComboBox.PresentItemStyle}"
                    ItemsSource="{Binding Path=SelectedItems, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ctl:MultiComboBox}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <ToggleButton
                    x:Name="PART_OpenButton"
                    Grid.Column="1"
                    Height="Auto"
                    Focusable="False"
                    IsChecked="{Binding Path=IsOpened, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                    Style="{StaticResource MultiComboBox.OpenButtonStyle}" />
                <Popup
                    x:Name="PART_Popup"
                    ctl:PopupBox.IsPositionUpdate="True"
                    AllowsTransparency="True"
                    IsOpen="{Binding IsOpened, RelativeSource={RelativeSource TemplatedParent}}"
                    Placement="Bottom"
                    PlacementTarget="{Binding ElementName=RootBorder}"
                    PopupAnimation="Slide"
                    SnapsToDevicePixels="True"
                    VerticalOffset="1">
                    <Border
                        x:Name="ExpansionBorder"
                        Width="{Binding ElementName=RootBorder, Path=ActualWidth}"
                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                        Background="{DynamicResource ReverseBackground}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <CheckBox
                                Grid.Row="0"
                                Height="Auto"
                                Content="{TemplateBinding SelectAllText}"
                                IsChecked="{Binding Path=IsSelectAll, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                            <Rectangle
                                Grid.Row="1"
                                Height="1"
                                Margin="0,1"
                                Fill="{TemplateBinding BorderBrush}" />
                            <ScrollViewer
                                Grid.Row="2"
                                Background="Transparent"
                                VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers />
    </ControlTemplate>

    <Style TargetType="ctl:MultiComboBox">
        <Setter Property="MinHeight" Value="{DynamicResource Height.Horizontal.Control}" />
        <Setter Property="Template" Value="{StaticResource MultiComboBox.ControlTemplate.Basic}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Default}" />
        <Setter Property="BorderThickness" Value="{DynamicResource BorderThickness.Thin}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource MultiComboBoxItem.Style.Basic}" />
        <Setter Property="PresenteItemStyle" Value="{StaticResource MultiComboBox.PresentItemStyle}" />
    </Style>
</ResourceDictionary>