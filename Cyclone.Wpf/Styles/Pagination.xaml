<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctl="clr-namespace:Cyclone.Wpf.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Cyclone.Wpf;component/Resources/CommonResources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style
        x:Key="Pagination.PrevNextRepeatButtonStyle"
        TargetType="RepeatButton" />

    <Style
        x:Key="Pagination.GotoPageButtonStyle"
        TargetType="Button" />

    <Style
        x:Key="Pagination.GotoPageNumberBoxStyle"
        TargetType="ctl:NumberBox" />

    <Style
        x:Key="Pagination.SelectListBoxItemStyleKey"
        TargetType="{x:Type ListBoxItem}">
        <Setter Property="Width" Value="{Binding Path=ActualHeight, RelativeSource={RelativeSource Mode=Self}}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5,0" />
        <Setter Property="Margin" Value="3,0" />
        <Setter Property="Background" Value="{DynamicResource DefaultBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DefaultBorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource CornerRadiusNormal}"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                        <ContentPresenter
                            x:Name="PART_ContentPresenter"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger
                Binding="{Binding .}"
                Value="···">
                <Setter Property="IsEnabled" Value="False" />
                <Setter Property="FontWeight" Value="Bold" />
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource HoverBorderBrush}" />
                <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource HoverForegroundBrush}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource CheckedBackgroundBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource AccentBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="Pagination.SelectListBoxStyle"
        TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ScrollViewer
                            Padding="{TemplateBinding Padding}"
                            Focusable="False">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsGrouping" Value="True">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate
        x:Key="BasicPaginationControlTemplate"
        TargetType="{x:Type ctl:Pagination}">
        <Border
            x:Name="RootBorder"
            Padding="{TemplateBinding Padding}"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True"
            UseLayoutRounding="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="PART_InfoTextBlock"
                    Grid.Column="0"
                    Margin="5,0,0,0"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                    Text="{Binding ItemCount, StringFormat=共 {0} 条, RelativeSource={RelativeSource TemplatedParent}}" />
                <StackPanel
                    Grid.Column="1"
                    Margin="10,0,5,0"
                    Orientation="Horizontal">
                    <TextBlock
                        VerticalAlignment="Center"
                        Text="每页 " />
                    <ComboBox
                        x:Name="PART_PerpageCountComboBox"
                        MinWidth="75"
                        SelectedValue="{Binding Path=PerpageCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                    <TextBlock
                        VerticalAlignment="Center"
                        Text=" 条" />
                </StackPanel>
                <StackPanel
                    Grid.Column="2"
                    Margin="5,0"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
                    <RepeatButton
                        x:Name="PART_PrevRepeatButton"
                        Width="{Binding ActualHeight, RelativeSource={RelativeSource Mode=Self}}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        Command="{x:Static ctl:Pagination.PrevCommand}"
                        Style="{StaticResource Pagination.PrevNextRepeatButtonStyle}">
                        <RepeatButton.Content>
                            <Path
                                x:Name="PrevPath"
                                Width="16"
                                Height="16"
                                Data="M18,49.1,68.3,79.5a4.6,4.6,0,0,0,4.4.1,4.3,4.3,0,0,0,2.2-3.8V15a4.3,4.3,0,0,0-2.2-3.8,4.3,4.3,0,0,0-4.4.1L18,41.7a4.3,4.3,0,0,0,0,7.4Zm0,0"
                                Fill="{DynamicResource DefaultForegroundBrush}"
                                Stretch="Uniform" />
                        </RepeatButton.Content>
                    </RepeatButton>
                    <ListBox
                        x:Name="PART_SelectListBox"
                        Margin="5,0"
                        ItemContainerStyle="{StaticResource Pagination.SelectListBoxItemStyleKey}"
                        ItemsSource="{TemplateBinding Pages}"
                        ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                        ScrollViewer.VerticalScrollBarVisibility="Hidden"
                        SelectedIndex="0"
                        Style="{StaticResource Pagination.SelectListBoxStyle}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                    <RepeatButton
                        x:Name="PART_NextRepeatButton"
                        Width="{Binding ActualHeight, RelativeSource={RelativeSource Mode=Self}}"
                        Command="{x:Static ctl:Pagination.NextCommand}"
                        Style="{StaticResource Pagination.PrevNextRepeatButtonStyle}">
                        <RepeatButton.Content>
                            <Path
                                x:Name="NextPath"
                                Width="16"
                                Height="16"
                                Data="M72.7,41.6,22.4,11.2a4.6,4.6,0,0,0-4.4-.1,4.4,4.4,0,0,0-2.2,3.8V75.7A4.3,4.3,0,0,0,18,79.5a4.3,4.3,0,0,0,4.4-.1L72.7,49a4.3,4.3,0,0,0,0-7.4Zm0,0"
                                Fill="{DynamicResource DefaultForegroundBrush}"
                                Stretch="Uniform" />
                        </RepeatButton.Content>
                    </RepeatButton>
                </StackPanel>

                <StackPanel
                    Grid.Column="3"
                    Margin="5,0"
                    Orientation="Horizontal">
                    <TextBlock
                        Margin="5,0"
                        VerticalAlignment="Center"
                        Text="前往" />
                    <ctl:NumberBox
                        x:Name="PART_GotoPageNumberBox"
                        MinWidth="50"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        IsVisibleButton="False"
                        IsVisibleSlider="False"
                        Maximum="{Binding Path=PageCount, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                        Minimum="1"
                        NumberStyle="Integer"
                        Value="{Binding Path=PageIndex, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                    <TextBlock
                        Margin="5,0"
                        VerticalAlignment="Center"
                        Text="页" />
                    <Button
                        x:Name="PART_GotoPageButton"
                        MinWidth="75"
                        Margin="5,0,0,0"
                        Style="{StaticResource Pagination.GotoPageButtonStyle}">
                        <Button.Content>
                            <Path
                                x:Name="GotoPath"
                                Width="16"
                                Height="16"
                                Data="M779.00800001 576L0 576 0 448 779.00800001 448 435.712 104.704 526.208 14.208 1024 512l-497.792 497.792-90.496-90.496z"
                                Fill="{DynamicResource DefaultForegroundBrush}"
                                Stretch="Uniform" />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </ControlTemplate>

    <Style TargetType="{x:Type ctl:Pagination}">
        <Setter Property="Template" Value="{StaticResource BasicPaginationControlTemplate}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>
</ResourceDictionary>