<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:Cyclone.Wpf.Converters"
    xmlns:ctl="clr-namespace:Cyclone.Wpf.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Cyclone.Wpf;component/Resources/CommonResources.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style TargetType="{x:Type ctl:Switch}">
        <!--  设置默认宽高，可根据需要调整  -->
        <Setter Property="TrackWidth" Value="50" />
        <Setter Property="Height" Value="{DynamicResource HorizontalControlHeight}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctl:Switch}">
                    <BulletDecorator>
                        <BulletDecorator.Bullet>
                            <Grid
                                Width="{TemplateBinding TrackWidth}"
                                Height="{TemplateBinding Height}"
                                SnapsToDevicePixels="True">
                                <Border
                                    x:Name="SwitchTrack"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource DefaultBorderBrush}"
                                    BorderThickness="1" />
                                <Border
                                    x:Name="SwitchThumb"
                                    Width="{Binding ActualHeight, RelativeSource={RelativeSource Mode=Self}}"
                                    Height="{Binding ActualHeight, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={x:Static conv:MathConverter.Scale}, ConverterParameter=0.8}"
                                    Margin="2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource DefaultBackgroundBrush}"
                                    BorderBrush="{DynamicResource DefaultBorderBrush}"
                                    BorderThickness="1" />
                            </Grid>
                        </BulletDecorator.Bullet>
                        <ContentPresenter
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            RecognizesAccessKey="True" />

                    </BulletDecorator>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="SwitchThumb" Property="Background" Value="{DynamicResource CheckedBorderBrush}" />
                            <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" />
                        </Trigger>
                        <Trigger SourceName="SwitchThumb" Property="IsMouseOver" Value="True">
                            <Setter TargetName="SwitchThumb" Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>